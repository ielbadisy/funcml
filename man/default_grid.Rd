% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{default_grid}
\alias{default_grid}
\title{Built-in tuning grid for a learner.}
\usage{
default_grid(model)
}
\arguments{
\item{model}{Learner id, as accepted by \code{\link[=fit]{fit()}}.}
}
\value{
A data frame of hyperparameter combinations, or \code{NULL} if the learner
has no tunable hyperparameters.
}
\description{
Returns the small default hyperparameter grid \code{compare(tune = TRUE)} uses for
a learner when no grid is supplied via \code{grids}. Learners with no tunable
hyperparameters (e.g. \code{glm}, \code{lda}, \code{qda}, \code{fda}, \code{naivebayes}, \code{gam}) return
\code{NULL}. The grids are intentionally coarse; retrieve one, edit it, and pass it
back through \code{grids} for a finer search.
}
\examples{
default_grid("ranger")
default_grid("glm")
}
