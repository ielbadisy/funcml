% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc_proc.R
\name{auc_ci}
\alias{auc_ci}
\title{AUC with a confidence interval.}
\usage{
auc_ci(
  truth,
  prob,
  positive = NULL,
  conf_level = 0.95,
  method = c("delong", "bootstrap"),
  boot_n = 2000L,
  digits = 4L
)
}
\arguments{
\item{truth}{Factor (or coercible to factor) of true class labels.}

\item{prob}{Predicted probability of the positive class, or a probability
matrix/data frame with one column per class level.}

\item{positive}{Optional positive class level. Defaults to the second
factor level.}

\item{conf_level}{Confidence level.}

\item{method}{\code{"delong"} (default, analytic) or \code{"bootstrap"}.}

\item{boot_n}{Number of bootstrap replicates when \code{method = "bootstrap"}.}

\item{digits}{Number of digits numeric columns are rounded to.}
}
\value{
A one-row data frame with \code{auc}, \code{conf_low}, \code{conf_high},
\code{conf_level}, and \code{method}.
}
\description{
Computes AUC together with a confidence interval, using \code{\link[pROC:ci.auc]{pROC::ci.auc()}}
as the backend (DeLong by default, or bootstrap).
}
\examples{
if (requireNamespace("pROC", quietly = TRUE)) {
  set.seed(1)
  truth <- factor(rbinom(100, 1, 0.4))
  prob <- runif(100)
  auc_ci(truth, prob)
}
}
