% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{metrics}
\alias{metrics}
\alias{rmse}
\alias{mae}
\alias{mse}
\alias{rsq}
\alias{medae}
\alias{mape}
\alias{logloss}
\alias{brier}
\alias{accuracy}
\alias{precision}
\alias{recall}
\alias{specificity}
\alias{f1}
\alias{balanced_accuracy}
\alias{auc}
\alias{calibration_curve}
\alias{ece}
\alias{mce}
\title{Regression and classification metrics.}
\usage{
rmse(truth, pred)

mae(truth, pred)

mse(truth, pred)

rsq(truth, pred)

medae(truth, pred)

mape(truth, pred)

logloss(truth, prob_matrix)

brier(truth, prob_matrix)

accuracy(truth, pred_class)

precision(truth, pred_class)

recall(truth, pred_class)

specificity(truth, pred_class)

f1(truth, pred_class)

balanced_accuracy(truth, pred_class)

auc(truth, prob)

calibration_curve(
  truth,
  prob,
  bins = 10,
  strategy = c("quantile", "uniform"),
  positive = NULL
)

ece(
  truth,
  prob,
  bins = 10,
  strategy = c("quantile", "uniform"),
  positive = NULL
)

mce(
  truth,
  prob,
  bins = 10,
  strategy = c("quantile", "uniform"),
  positive = NULL
)
}
\arguments{
\item{truth}{Observed outcomes.}

\item{pred}{Predicted numeric values or class labels.}

\item{prob_matrix}{Matrix or vector of predicted probabilities (classification).}

\item{pred_class}{Predicted class labels (classification).}

\item{prob}{Probability vector for binary classification metrics.}

\item{bins}{Number of bins for calibration summaries.}

\item{strategy}{Binning strategy: \code{"quantile"} or \code{"uniform"}.}

\item{positive}{Optional positive/event class for binary classification.}
}
\value{
Numeric scalar metric.
}
\description{
Base R implementations used across evaluation and interpretation utilities.
}
\examples{
truth_reg <- c(3, 5, 2.5, 7)
pred_reg <- c(2.8, 4.9, 2.7, 6.8)
rmse(truth_reg, pred_reg)
mae(truth_reg, pred_reg)
mse(truth_reg, pred_reg)
rsq(truth_reg, pred_reg)
medae(truth_reg, pred_reg)
mape(truth_reg, pred_reg)

truth_cls <- factor(c("no", "yes", "yes", "no"), levels = c("no", "yes"))
pred_cls <- factor(c("no", "yes", "no", "no"), levels = levels(truth_cls))
prob_cls <- cbind(
  no = c(0.8, 0.2, 0.6, 0.7),
  yes = c(0.2, 0.8, 0.4, 0.3)
)
logloss(truth_cls, prob_cls)
brier(truth_cls, prob_cls)
accuracy(truth_cls, pred_cls)
precision(truth_cls, pred_cls)
recall(truth_cls, pred_cls)
specificity(truth_cls, pred_cls)
f1(truth_cls, pred_cls)
balanced_accuracy(truth_cls, pred_cls)
auc(truth_cls, prob_cls[, "yes"])
calibration_curve(truth_cls, prob_cls[, "yes"])
ece(truth_cls, prob_cls[, "yes"])
mce(truth_cls, prob_cls[, "yes"])
}
