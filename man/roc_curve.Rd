% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roc_proc.R
\name{roc_curve}
\alias{roc_curve}
\title{ROC curve.}
\usage{
roc_curve(truth, prob, positive = NULL)
}
\arguments{
\item{truth}{Factor (or coercible to factor) of true class labels.}

\item{prob}{Predicted probability of the positive class, or a probability
matrix/data frame with one column per class level.}

\item{positive}{Optional positive class level. Defaults to the second
factor level.}
}
\value{
A \code{funcml_roc} object: a list with a \code{curve} data frame
(\code{threshold}, \code{sensitivity}, \code{specificity}), the AUC, and the
underlying \code{pROC::roc} object (\code{proc}).
}
\description{
Computes the receiver operating characteristic curve for binary
classification predictions, using \code{\link[pROC:roc]{pROC::roc()}} as the backend.
}
\examples{
if (requireNamespace("pROC", quietly = TRUE)) {
  set.seed(1)
  truth <- factor(rbinom(100, 1, 0.4))
  prob <- runif(100)
  roc_obj <- roc_curve(truth, prob)
  roc_obj$curve
}
}
