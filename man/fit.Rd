% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{fit}
\alias{fit}
\title{Fit a model using the funcml interface.}
\usage{
fit(
  formula,
  data,
  model,
  spec = NULL,
  seed = NULL,
  na_action = stats::na.fail,
  ...
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{Data frame.}

\item{model}{Learner id (see \code{learners()}).}

\item{spec}{Optional list of hyperparameters for the learner.}

\item{seed}{Optional seed for reproducibility.}

\item{na_action}{NA handling passed to \code{model.frame}/\code{model.matrix} (default \code{stats::na.fail}).}

\item{...}{Additional parameters merged into \code{spec}.}
}
\value{
An object of class \code{funcml_fit}.
}
\description{
Registered learner ids currently include:
regression and classification: \code{glm}, \code{rpart}, \code{glmnet}, \code{ranger}, \code{nnet},
\code{e1071_svm}, \code{randomForest}, \code{gbm}, \code{kknn}, \code{ctree}, \code{cforest},
\code{lightgbm}, \code{xgboost}, \code{stacking}, \code{superlearner};
regression plus binary classification: \code{gam}, \code{bart};
classification only: \code{C50}, \code{naivebayes}, \code{fda}, \code{lda}, \code{qda};
binary classification only: \code{adaboost};
regression plus binary classification: \code{earth};
regression only: \code{pls}.
}
\details{
The learner engine packages are installed with \code{funcml}, so the advertised
registry is intended to be available after a standard installation.
}
\examples{
fit_obj <- fit(mpg ~ wt + hp, data = mtcars, model = "glm")
predict(fit_obj, newdata = mtcars[1:3, , drop = FALSE])
}
