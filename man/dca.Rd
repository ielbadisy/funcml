% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dca.R
\name{dca}
\alias{dca}
\title{Decision curve analysis: net benefit.}
\usage{
dca(truth, prob, positive = NULL, thresholds = seq(0.01, 0.99, by = 0.01))
}
\arguments{
\item{truth}{Factor (or coercible to factor) of true class labels.}

\item{prob}{Predicted probability of the positive class, or a probability
matrix/data frame with one column per class level.}

\item{positive}{Optional positive class level. Defaults to the second
factor level.}

\item{thresholds}{Numeric vector of risk thresholds in (0, 1).}
}
\value{
A data frame with \code{threshold}, \code{model}, \code{treat_all}, and
\code{treat_none} net benefit columns.
}
\description{
Computes net benefit across a range of risk thresholds for a binary
classifier's predicted probabilities, alongside the "treat all" and
"treat none" reference strategies (Vickers and Elkin, 2006).
}
\examples{
set.seed(1)
truth <- factor(rbinom(200, 1, 0.3))
prob <- pmin(pmax(rnorm(200, mean = ifelse(truth == 1, 0.6, 0.3), sd = 0.15), 0), 1)
dca(truth, prob)
}
