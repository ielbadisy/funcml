% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpret.R
\name{interpret-local-methods}
\alias{interpret-local-methods}
\alias{plot.funcml_local}
\alias{print.funcml_local}
\alias{summary.funcml_local}
\title{Local surrogate result methods.}
\usage{
\method{plot}{funcml_local}(x, ...)

\method{print}{funcml_local}(x, ...)

\method{summary}{funcml_local}(object, ...)
}
\arguments{
\item{x}{A \code{funcml_local} object.}

\item{...}{Additional arguments passed to the underlying method.}

\item{object}{A \code{funcml_local} object.}
}
\value{
\code{plot()} returns a \code{ggplot2} object. \code{print()} returns the input
object invisibly. \code{summary()} returns the local explanation payload
invisibly.
}
\description{
These methods provide the standard \code{print()}, \code{summary()}, and \code{plot()}
interfaces for \code{funcml_local} objects returned by \code{interpret(method = "local")}
or \code{interpret(method = "lime")}.
}
\examples{
fit_obj <- fit(mpg ~ wt + hp + disp, data = mtcars, model = "rpart",
  spec = list(cp = 0.01, minsplit = 5))
local_obj <- interpret(
  fit = fit_obj,
  data = mtcars,
  method = "local",
  newdata = mtcars[1, , drop = FALSE],
  k = 2
)
print(local_obj)
summary(local_obj)
plot(local_obj)
}
