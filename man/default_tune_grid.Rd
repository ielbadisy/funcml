% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune-grids.R
\name{default_tune_grid}
\alias{default_tune_grid}
\title{Default tuning grid for a learner.}
\usage{
default_tune_grid(model, data = NULL, formula = NULL)
}
\arguments{
\item{model}{Learner id, as in \code{\link[=list_learners]{list_learners()}}.}

\item{data}{Optional data frame, used to size data-dependent parameters.}

\item{formula}{Optional model formula, used together with \code{data}.}
}
\value{
A data frame with one row per configuration, or \code{NULL} for learners
with no tunable hyperparameters (\code{glm}, \code{lda}, \code{qda}, \code{fda}, \code{stacking},
\code{superlearner}).
}
\description{
Returns a small, sensible hyperparameter grid for a learner, so that
\code{\link[=tune]{tune()}} and \code{\link[=compare]{compare()}} with \code{tune = TRUE} work without the user writing
a grid. Grids that depend on the data (for example \code{mtry}, \code{ncomp}, \code{k}, and
the SVM \code{gamma}) are sized from the number of encoded predictors and the
number of rows of \code{data}. Parameter names are the ones of each learner's
\code{spec} (see \code{\link[=fit]{fit()}}).
}
\examples{
default_tune_grid("rpart")
default_tune_grid("ranger", data = mtcars, formula = mpg ~ .)
}
