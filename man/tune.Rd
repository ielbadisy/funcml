% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune.R
\name{tune}
\alias{tune}
\title{Hyperparameter tuning via grid or random search.}
\usage{
tune(
  data,
  formula,
  model,
  grid,
  resampling = cv(5),
  metric = NULL,
  type = NULL,
  search = c("grid", "random"),
  n_evals = NULL,
  outer_resampling = NULL,
  seed = NULL,
  ncores = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame.}

\item{formula}{Model formula.}

\item{model}{Learner id.}

\item{grid}{Data frame of hyperparameter combinations.}

\item{resampling}{Resampling object.}

\item{metric}{Metric to optimize.}

\item{type}{Prediction type override.}

\item{search}{Search strategy: \code{"grid"} or \code{"random"}.}

\item{n_evals}{Maximum number of configurations to evaluate when
\code{search = "random"}.}

\item{outer_resampling}{Optional outer resampling object. When supplied,
\code{tune()} performs nested resampling and reports outer-fold performance
estimates for the tuned model-selection procedure.}

\item{seed}{Optional seed.}

\item{ncores}{Optional number of CPU cores used for tuning tasks. \code{NULL} or
\code{1} runs sequentially.}

\item{...}{Passed to \code{fit()}.}
}
\value{
A \code{funcml_tune} object.
}
\description{
Hyperparameter tuning via grid or random search.
}
\examples{
tune_obj <- tune(
  data = mtcars,
  formula = mpg ~ wt + hp,
  model = "rpart",
  grid = expand.grid(cp = c(0.001, 0.01), minsplit = c(5, 10)),
  resampling = cv(3, seed = 1),
  metric = "rmse"
)
tune_obj$best
}
